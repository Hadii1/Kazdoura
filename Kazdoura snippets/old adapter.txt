

public class Info_Adapter extends RecyclerView.Adapter<MyAdapter.MyViewHolder> {
    private static final String TAG = "InfoAdapterClass";
    Context mContext;

        public Info_Adapter(FirestoreRecyclerOptions<Info> options) {
        super(options);
    }



    
    /*protected void onBindViewHolder(InfoHolder infoHolder, int i, Info info) {
        infoHolder.name.setText(info.getName());
        infoHolder.rating.setText(info.getRating());
        infoHolder.frontDescription.setText(info.getFrontDescription());
        infoHolder.dot.setAnimation(AnimationUtils.loadAnimation(mContext,R.anim.fade_in));
        infoHolder.storeImage.setAnimation(AnimationUtils.loadAnimation(mContext,R.anim.fade_transition_animation));
        infoHolder.cardConstraintLayout.setAnimation(AnimationUtils.loadAnimation(mContext,R.anim.fade_scale_animation));
        Log.d(TAG,"In onBindViewHolder");*/
    }

    @NonNull
    @Override
    public InfoHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View v = LayoutInflater.from(parent.getContext()).inflate(R.layout.card_view,parent,false);
        mContext = parent.getContext();
            return new InfoHolder(v);
    }

    @Override
    public void onBindViewHolder(@NonNull MyAdapter.MyViewHolder holder, int position) {
        
    }

    @Override
    public int getItemCount() {
        return 0;
    }

    class InfoHolder extends RecyclerView.ViewHolder {
        TextView name;
        TextView rating;
        TextView frontDescription;
        ImageButton dot;
        ImageView storeImage;
        ConstraintLayout cardConstraintLayout;

        public InfoHolder(@NonNull View itemView) {
            super(itemView);
            name = itemView.findViewById(R.id.storeName);
            rating = itemView.findViewById(R.id.rating);
            frontDescription = itemView.findViewById(R.id.frontDescription);
            dot = itemView.findViewById(R.id.dot);
            storeImage = itemView.findViewById(R.id.storeImage);
            cardConstraintLayout = itemView.findViewById(R.id.cardConstraintLayout);
        }
    }
}
